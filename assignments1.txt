Express Middleware

1.	Create a route in express application to send login form to the user. 
	Login form should have bacground image.
	
2.	Create a custom middleware which logs the information about the request time 
	and request URL received from the client